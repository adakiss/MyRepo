apply plugin: 'java'
apply plugin: 'war'

repositories { mavenCentral() }

war { archiveName 'fs-weblayer.war' }

dependencies {
	providedCompile project(':fs-ejbservice')
	providedCompile group: 'javax', name: 'javaee-api', version:'6.0'
}